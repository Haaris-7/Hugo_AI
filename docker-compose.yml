services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hugo
      POSTGRES_USER: hugo
      POSTGRES_PASSWORD: hugo
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hugo"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - hugo-postgres:/var/lib/postgresql/data

  api:
    build:
      context: .
      target: prod
    environment:
      HUGO_DATABASE_URL: postgresql+psycopg://hugo:hugo@postgres:5432/hugo
      HUGO_INLINE_JOBS: "false"
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  migrate:
    build:
      context: .
      target: prod
    command: ["alembic", "upgrade", "head"]
    env_file:
      - path: .env.example
      - path: .env
        required: false
    environment:
      HUGO_DATABASE_URL: postgresql+psycopg://hugo:hugo@postgres:5432/hugo
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build:
      context: .
      target: prod
    command: ["python", "-m", "hugo.worker"]
    environment:
      HUGO_DATABASE_URL: postgresql+psycopg://hugo:hugo@postgres:5432/hugo
      HUGO_INLINE_JOBS: "false"
      HUGO_HERMES_CRON_ACTIVE: "true"
    volumes:
      - ./.env:/app/.env:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  dev:
    build:
      context: .
      target: dev
    environment:
      HUGO_DATABASE_URL: postgresql+psycopg://hugo:hugo@postgres:5432/hugo
      HUGO_INLINE_JOBS: "true"
    volumes:
      - ./.env:/app/.env
      - ./backend:/app/backend
      - ./backend/tests:/app/backend/tests
      - ./hermes-plugin:/app/hermes-plugin
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-}
    environment:
      HUGO_API_BASE_URL: http://api:8000
      HUGO_API_TOKEN: ${HUGO_API_TOKEN:-change-me}
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_started

volumes:
  hugo-postgres:
